import { UsuarioHorarioPipe } from './usuario-horario.pipe';

describe('UsuarioHorarioPipe', () => {
  it('create an instance', () => {
    const pipe = new UsuarioHorarioPipe();
    expect(pipe).toBeTruthy();
  });
});
